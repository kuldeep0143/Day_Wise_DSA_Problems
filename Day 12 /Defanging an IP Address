public class solution{
  Public static String GenerateDefangedIP(String str ) {
    String res = "";
    for(int i = 0;i<str.length();i++) {
      char c = str.charAt(i);
    if(c == '.') {
       res += "[.]";
    } else {
      res+=c;
  }
  }
 return res;
}
}


